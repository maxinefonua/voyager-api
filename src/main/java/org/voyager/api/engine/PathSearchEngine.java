package org.voyager.api.engine;

import org.voyager.api.model.graph.RouteGraph;
import org.voyager.commons.model.path.Path;

import java.util.List;

// purely algo - logic - no database dependencies
public class PathSearchEngine {
    List<Path> findAllPaths(RouteGraph routeGraph) {
        return List.of();
    }
}
